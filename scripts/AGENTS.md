This folder contains server lifecycle scripts.

Current scripts:
- `start-server-mac.sh`: builds Docker image and starts local container on port 8000
- `stop-server-mac.sh`: stops and removes the local container

Future additions can include equivalent scripts for Windows and Linux.